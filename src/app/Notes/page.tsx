import React from 'react';
import NoteEditor from '../Components/NotesComponents/NoteEditor'; // Adjust path if necessary

const NotesPage: React.FC = () => {
    return (
      <div>
        <NoteEditor></NoteEditor>
      </div>
    );
  };

export default NotesPage;
